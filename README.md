# 🍃 Счетчик Кальянов — Telegram бот

Телеграм-бот для учета кальянов в кальянной: управление сменами, заявки на кальяны, статусы, уведомления и профиль с ролью.

## ✨ Что умеет бот

- Открывать и закрывать смены
- Добавлять кальяны с выбором типа, силы, холодности и комментариев
- Принимать кальяны мастером и перевести их в статус готовности
- Отображать список текущих кальянов и историю смен
- Редактировать тип и стол кальяна
- Удалять кальяны
- Уведомлять участников о новых кальянах
- Хранить данные в SQLite
- Показывать профиль пользователя с ролью и статистикой
- Работать в московском времени (UTC+3)

## 🧩 Роли

- `admin` — полный доступ к админской панели и управлению сменами
- `manager` — управление заявками и редактирование кальянов
- `hookah_master` — добавление и принятие кальянов, профильная статистика
- `supervisor` — наблюдение за сменой и отчетность
- `member` — участие в смене и просмотр данных

## 📌 Профиль пользователя

В профиле отображается:

- Статус роли
- Имя пользователя из базы
- Статистика кальянов для кальянного мастера:
  - сколько кальянов забито в твоих сменах
  - количество стандартных и сигар
  - общее число кальянов в системе
- Смены всего и за текущий месяц
- Переключение уведомлений

## 🚀 Установка

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/username/schetchik-kalikov.git
   cd "Schetchik Kalиков"
   ```
2. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Скопируйте шаблон конфигурации:
   ```bash
   copy .env.example .env
   ```
5. Отредактируйте `.env` и укажите `BOT_TOKEN` и `ADMIN_USER_ID`.

## ⚙️ Конфигурация

Пример `.env`:

```env
BOT_TOKEN=ваш_токен_здесь
ADMIN_USER_ID=ваш_telegram_user_id
# DB_PATH=hookah_bot.db
# LOG_LEVEL=INFO
```

## ▶️ Запуск

```bash
python bot.py
```

## 📝 Как работать

### Добавление кальяна

1. Нажмите **Добавить кальян**
2. Выберите тип
3. Выберите стол
4. Выберите силу (1–10)
5. Выберите холодность
6. Добавьте комментарий или отправьте `нет`

### Управление кальянами

- Откройте **Текущие кальяны**
- Выберите кальян из списка
- Измените тип, стол или удалите кальян

### Админская панель

- Откройте смену
- Назначайте роли участникам
- Удаляйте смену
- Просматривайте расписание и состав команды

## 🗂️ Структура проекта

```text
├── bot.py
├── .env.example
├── README.md
├── requirements.txt
└── src/
    ├── database.py
    ├── handlers.py
    └── keyboards.py
```

## 🛠️ Архитектура

- `bot.py` — запуск бота и загрузка конфигурации
- `src/database.py` — хранение, запросы и логика БД
- `src/handlers.py` — команды, callback и FSM
- `src/keyboards.py` — inline-клавиатуры

## 📦 Зависимости

```bash
pip install -r requirements.txt
```

Пример актуальных зависимостей:

```text
aiogram==3.24.0
python-dotenv==1.0.1
```

## 🚨 Типичные ошибки

- Токен не указан в `.env`
- Неверный `ADMIN_USER_ID`
- Бот не запущен

### Если бот не отвечает

- Проверьте, что `bot.py` запущен
- Убедитесь, что сеть работает
- Перезапустите бота: `Ctrl+C` и снова `python bot.py`

## 📝 Лицензия

MIT License

## 👨‍💻 Автор

Разработано для кальянной